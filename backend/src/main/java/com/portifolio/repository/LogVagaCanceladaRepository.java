package com.portifolio.repository;

import com.portifolio.model.LogVagaCancelada;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LogVagaCanceladaRepository extends JpaRepository<LogVagaCancelada, Long> {
}
